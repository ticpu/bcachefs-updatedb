[package]
name = "bcachefs-updatedb"
version = "0.1.0"
edition = "2024"
description = "updatedb for bcachefs: builds plocate databases from the dirents btree instead of walking the tree"
license = "GPL-2.0-only"
repository = "https://github.com/ticpu/bcachefs-updatedb"
authors = ["Jérôme Poulin <jeromepoulin@gmail.com>"]

[dependencies]
clap = { version = "4", features = ["derive"] }
libc = "0.2"

[profile.release]
opt-level = 3
